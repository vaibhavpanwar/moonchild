import React from 'react';
import EditForm from '../../../components/Forms/countries/editForm';
const editCountry = ({history}) => {
  return <EditForm history={history} />;
};

export default editCountry;
