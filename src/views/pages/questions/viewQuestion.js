import React from 'react';
import ViewForm from '../../../components/Forms/questions/viewForm';
const ViewQuestion = ({history}) => {
  return <ViewForm history={history} />;
};

export default ViewQuestion;
