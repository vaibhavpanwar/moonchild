import React from 'react';
import AddForm from '../../../components/Forms/sub-categories/addForm';

const addSubCategory = ({history}) => {
  return <AddForm history={history} />;
};

export default addSubCategory;
