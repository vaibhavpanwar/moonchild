import React from 'react';
import AddForm from '../../../components/Forms/countries/addForm';
const addCountry = ({history}) => {
  return <AddForm history={history} />;
};

export default addCountry;
