import React from 'react';
import EditForm from '../../../components/Forms/banners/editForm';
const editBanner = ({history}) => {
  return <EditForm history={history} />;
};

export default editBanner;
