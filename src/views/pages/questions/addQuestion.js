import React from 'react';
import AddForm from '../../../components/Forms/questions/addForm';
const addQuestion = ({history}) => {
  return <AddForm history={history} />;
};

export default addQuestion;
