import React from 'react';
import ViewForm from '../../../components/Forms/categories/viewForm';
const viewCategory = ({history}) => {
  return <ViewForm history={history} />;
};

export default viewCategory;
