import React from 'react';
import ViewForm from '../../../components/Forms/countries/viewForm';
const viewCountry = ({history}) => {
  return <ViewForm history={history} />;
};

export default viewCountry;
