import React from 'react';
import EditForm from '../../../components/Forms/questions/editForm';
const EditQuestion = ({history}) => {
  return <EditForm history={history} />;
};

export default EditQuestion;
