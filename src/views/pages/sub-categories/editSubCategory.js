import React from 'react';
import EditForm from '../../../components/Forms/sub-categories/editForm';
const editSubCategory = ({history}) => {
  return <EditForm history={history} />;
};

export default editSubCategory;
